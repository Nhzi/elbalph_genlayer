"""Shared pytest config for direct-mode tests.

`genlayer-test` provides the `direct_vm`, `direct_deploy`, and `direct_alice`/
`direct_bob`/etc. fixtures automatically — this file is intentionally empty
beyond standard imports so pytest discovers the package.
"""
